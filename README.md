# Prototype

Standalone interactive prototype — separate from the portfolio project.

## Local preview

```bash
npm run dev
```

Open `http://localhost:3000`

## Deploy

Deployed independently on Vercel. Push to GitHub or run `npx vercel deploy --prod`.
